#:schema https://dystroy.org/bacon/.bacon.schema.json

# This is a configuration file for the bacon tool
#
# Complete help on configuration: https://dystroy.org/bacon/config/
#
# You may check the current default at
#   https://github.com/Canop/bacon/blob/main/defaults/default-bacon.toml

default_job = "nextest"
env.CARGO_TERM_COLOR = "always"

[jobs.check]
command = ["cargo", "check"]
need_stdout = false

# Run clippy on the default target
[jobs.clippy]
command = ["cargo", "clippy"]
need_stdout = false

[jobs.nextest]
command = [
    "cargo", "+nightly", "nextest", "run",
    "-Z", "codegen-backend",           # Unlocks the unstable backend feature flag
    "-j", "8",
    "--timings"
]
need_stdout = true
analyzer = "nextest"

[jobs.nextest.env]
CARGO_PROFILE_DEV_CODEGEN_BACKEND = "cranelift"  # Dynamically assigns cranelift to dev builds
RUSTC_WRAPPER = "sccache"

[jobs.doc]
command = ["cargo", "doc", "--no-deps"]
need_stdout = false

# If the doc compiles, then it opens in your browser and bacon switches
# to the previous job
[jobs.doc-open]
command = ["cargo", "doc", "--no-deps", "--open"]
need_stdout = false
on_success = "back" # so that we don't open the browser at each change

# You can run your application and have the result displayed in bacon,
# if it makes sense for this crate.
[jobs.run]
command = [
    "cargo", "run",
    # put launch parameters for your program behind a `--` separator
]
need_stdout = true
allow_warnings = true
background = true

# Run your long-running application (eg server) and have the result displayed in bacon.
# For programs that never stop (eg a server), `background` is set to false
# to have the cargo run output immediately displayed instead of waiting for
# program's end.
# 'on_change_strategy' is set to `kill_then_restart` to have your program restart
# on every change (an alternative would be to use the 'F5' key manually in bacon).
# If you often use this job, it makes sense to override the 'r' key by adding
# a binding `r = job:run-long` at the end of this file .
# A custom kill command such as the one suggested below is frequently needed to kill
# long running programs (uncomment it if you need it)
[jobs.run-long]
command = [
    "cargo", "run",
    # put launch parameters for your program behind a `--` separator
]
need_stdout = true
allow_warnings = true
background = false
on_change_strategy = "kill_then_restart"
# kill = ["pkill", "-TERM", "-P"]

# This parameterized job runs the example of your choice, as soon
# as the code compiles.
# Call it as
#    bacon ex -- my-example
[jobs.ex]
command = ["cargo", "run", "--example"]
need_stdout = true
allow_warnings = true

# 1. Standard build, but with timings enabled
[jobs.build-timings]
command = [
    "cargo", "build",
    "--timings"
]
need_stdout = true

# 2. "Super Optimized" custom job
[jobs.superoptimized]
command = [
    "cargo", "+nightly", "build", 
    "-Z", "codegen-backend",           # Unlocks the unstable backend feature flag
    "-j", "8",                        
    "--timings"
]
need_stdout = true

[jobs.superoptimized.env]
CARGO_PROFILE_DEV_CODEGEN_BACKEND = "cranelift"  # Dynamically assigns cranelift to dev builds
RUSTC_WRAPPER = "sccache"

[keybindings]
# Remap 'c' or choose custom keys to trigger your profiling layouts on a whim
t = "job:build-timings"   # Press 't' in bacon to run basic build + timings
s = "job:superoptimized"  # Press 's' in bacon to engage super-optimized hyperdrive
c = "job:clippy" # comment this to have 'c' run clippy on only the default target
v = "job:nextest"