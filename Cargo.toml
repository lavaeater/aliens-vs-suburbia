#cargo-features = ["codegen-backend"]

[package]
name = "aliens-vs-suburbia"
version = "0.1.0"
edition = "2024"
exclude = ["dist", "build", "assets", "credits"]

[dependencies]
bevy = { version = "0.15", default-features = false, features = [
    "animation",
    "bevy_asset",
    "bevy_state",
    "bevy_color",
    "bevy_gilrs",
    "bevy_scene",
    "bevy_winit",
    "bevy_core_pipeline",
    "bevy_pbr",
    "bevy_gltf",
    "bevy_render",
    "bevy_sprite",
    "bevy_text",
    "bevy_ui",
    "multi_threaded",
    "png",
    "hdr",
    "x11",
    "bevy_gizmos",
    "tonemapping_luts",
    "smaa_luts",
    "default_font",
    "webgl2",
    "sysinfo_plugin",
    "bevy_dev_tools"
] }
bevy_reflect = { version = "0.15.3", default-features = false, features = ["default"] }
bevy_reflect_derive = "0.15.3"
bevy_rand = "0.9.0"
avian3d = "0.2.1"
big-brain = "0.22.0"
flagset = "0.4.4"
pathfinding = "4.6.0"
itertools = "0.14.0"
bevy_mod_outline = "0.9.1"
bevy-inspector-egui = "0.30.0"
bevy_atmosphere = "0.12.2"
image = "0.25.6"
noise = "0.9.0"
bevy_panorbit_camera = "0.25.0"
# Enable max optimizations for dependencies, but not for our code:
[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true